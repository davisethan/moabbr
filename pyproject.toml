[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "moabbr"
version = "0.0.2"

[tool.hatch.build.targets.wheel]
packages = ["moabbr"]

[tool.sqlfluff.core]
dialect = "duckdb"
